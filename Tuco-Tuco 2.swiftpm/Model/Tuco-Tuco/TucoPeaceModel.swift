import SwiftUI

struct TucoPeaceModel {
    static let originalPosition = CGPoint(x: UIScreen.main.bounds.midX + formatSize(value: 70, with: .width), y: UIScreen.main.bounds.midY)
    static let width = formatSize(value: 600, with: .width)
}

